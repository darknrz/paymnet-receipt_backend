services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=${POSTGRES_DB:-payment-receipt_db}'
      - 'POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-Toloveru212}'
      - 'POSTGRES_USER=${POSTGRES_USER:-postgres}'
    ports:
      - '5434:5432'